my-app {
  routes {
    # If ask takes more time than this to complete the request is failed
    ask-timeout = 5s
  }

  # wallet server address
  wallet-server {
    address = "http://localhost:8082"
  }

  # restaurent server address
  restaurant-server {
    address = "http://localhost:8080"
  }
}